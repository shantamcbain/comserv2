# Updated guidance: canonical definitions live at /static/config/theme_definitions.json
 
Note: Theme system now reads from the canonical theme_definitions.json file in static/config/.
All code paths should reference this single source of truth for themes and mappings.
