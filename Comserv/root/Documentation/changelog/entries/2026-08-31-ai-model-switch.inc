<div id="2026-08-31-ai-model-switch" class="cl-entry">
<h3>2026-08-31 — AI model switch: grok-composer-2.5-fast → hy4-preview (OpenRouter)</h3>
<p class="doc-body"><strong>What.</strong> User switched the Hermes model from <code>grok-composer-2.5-fast</code> (xAI OAuth) to <code>hy4-preview</code> via OpenRouter. My role identification adjusts accordingly.</p>
<p class="doc-body"><strong>Why.</strong> User-initiated provider change; no code defect. The config at <code>.hermes/config.yaml</code> still reads <code>grok-composer-2.5-fast</code> on the host; will require a deliberate update to reflect the new default.</p>
<p class="doc-body"><strong>Files.</strong> <code>.hermes/config.yaml</code> (host-level; user edits). Documentation notes below. No template or BOM work affected.</p>
</div>