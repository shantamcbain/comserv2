# Comserv — AI Guidance (`.hermes.md`)

Auto-loaded every Hermes session. This is the **single consolidated entry point** for
AI/agent work on the Comserv2 codebase (Perl/Catalyst + DBIx::Class + TT templates).
AGENTS.md is a thin pointer to this file; keep detailed guidance here.

> **Note:** This file is the auto-loaded AI-guidance entry point (consolidated
> conventions). It is NOT the documentation master plan. The real coordinator that links
> every branch/project plan is the in-app `MASTER_PLAN.tt`. The documentation-specific
> coordinator (audit + work items + linked plans) is `DocumentationMasterPlan.tt`.
> Both are under `Comserv/root/Documentation/` and rendered at
> `/Documentation/MASTER_PLAN` and `/Documentation/DocumentationMasterPlan`. The AI-role
> standing process lives at `.hermes/docs-maintenance-plan.md`.

---

## 1. Project at a glance

| | |
|---|---|
| **Language / Framework** | Perl 5 + Catalyst (MVC) |
| **ORM / DB** | DBIx::Class over MySQL (per-`SiteName` databases) |
| **Templates** | Template Toolkit (`.tt`) |
| **Primary access** | `workstation.local:3001` |
| **Default workflow** | Analyze → Plan → Diff → Apply |
| **Repo layout** | `Comserv/lib/Comserv/{Controller,Model,Util}` · `Comserv/root` (templates+static) · `Documentation/` (rendered `.tt` docs) · `script/`, `services/`, `infrastructure/` |

### Branch discipline (load-bearing — respect ownership)
- `main` / `master` — production-stable. Do not originate schema/Result-class/DB-table
  work here; that belongs on the `schema` branch. Never `git switch main` inside a worktree.
- `aisystem`, `ai`, `aichatsystem-*` — AI/voice code ownership.
- `schema`, `schema-*` — DB tables, Result classes, schema-compare authority.
- `Documentation` (this branch) — documentation structure, rendered `.tt` docs, and sync tooling.
- `InventoryAccounting`, `ENCY` — their own domains.

If a task crosses ownership (e.g. AI feature needs a new table), **build the AI side
reusing existing tables**; hand new-table/hand-DDL needs to the schema owner. Never
hand-write DDL — create/repair DB structure **only via the app schema-compare flow**.

---

## 2. Documentation system (the core of this role)

Comserv keeps its user/admin docs as **`.tt` files rendered in-app**, with parallel
**`.SUMMARY.md` / `.md` files for AI context**. The two are different audiences:

| Audience | Format | Location | Notes |
|---|---|---|---|
| Rendered web page | `.tt` with YAML `META` block | `Comserv/root/Documentation/<Module>/…` and `Documentation/<Module>/…` | Must use `var(--*)` theming; never hardcode colors |
| AI context (plain) | `.SUMMARY.md` (<2KB) | `Documentation/<Module>/SUMMARY.md` | Quick structured context for agents |
| Module index | `index.tt` | `…/<Module>/index.tt` | Rendered landing page with a `META` block |
| Changelog | per-entry `.inc` fragments | `Comserv/root/Documentation/changelog/entries/` | See §3 — never hand-edit root `CHANGELOG.tt` (it is a generated reader) |

### `.tt` page contract (required on every created/edited doc)
- **PageVersion header** (top of file):
  `[% PageVersion = 'path/file.tt,v 0.01 2025/MM/DD shanta Exp shanta ' %]`
  plus the debug-mode echo block:
  `[% IF c.session.debug_mode == 1 %][% PageVersion %][% END %]`
- **YAML `META` block** with at least `title`. Prefer also `description`, `category`,
  `roles`, `TemplateType`, `last_updated`, `page_version`, `site_specific`.
- **Theming:** use CSS variables — `var(--text-color)`, `var(--primary-color)`,
  `var(--link-color)`, etc. (from `base.css`). Pick the correct base template
  (`ApplicationTtTemplate` or `DocumentationTtTemplate`).
- **No `.md` under `Comserv/root/Documentation/`** for rendered pages — convert any
  stray `.md` there to `.tt`. (`.md` is acceptable only for AI-context stubs and
  session handoffs, per existing convention.)

### Automation that already exists
- `Documentation/script/sync-docs.sh` — scans Controllers/Models, refreshes the
  auto-detected controller index inside `CLAUDE.md` / `Documentation/SUMMARY.md`.
  Run `bash Documentation/script/sync-docs.sh --dry-run` to preview.
- Post-commit hook (`Documentation/script/post-commit-hook.sh`) runs the sync
  automatically on commits touching `.pm` files under `Controller/` or `Model/`.

---

## 3. Documentation compliance — DO THIS ON EVERY CHANGE

1. **Code/doc changes** → add a changelog **fragment**, do NOT hand-edit the
   root `CHANGELOG.tt` (that file is a *generated reader* rebuilt from the
   fragments). Create one file:
   `Comserv/root/Documentation/changelog/entries/YYYY-MM-DD-slug.inc`
   Format (matches every other entry):
   ```
   <div id="YYYY-MM-DD-slug" class="cl-entry">
   <h3>YYYY-MM-DD — Short title</h3>
   <p class="doc-body"><strong>What.</strong> …</p>
   <p class="doc-body"><strong>Why.</strong> …</p>
   <p class="doc-body"><strong>Files.</strong> <code>…</code></p>
   </div>
   ```
   This directory exists on **every branch**, so the note is visible everywhere
   and adding a file never collides on merge. The reader at
   `/Documentation/CHANGELOG` is generated by `Comserv::Util::Changelog`.
   Keep each entry specific: symptom, root cause, solution, files touched.
2. **Member / user how-to** → edit the stable `Documentation/guides/*.tt` (or the
   relevant module `index.tt`) **in place**; prefer improving existing docs over
   creating new ones.
3. **New module docs** → add an `index.tt` with a `META` block AND a sibling
   `SUMMARY.md` for AI context.
4. **Decision: `.tt` vs DB vs both** → made ad hoc per topic. Use the in-app
   documentation controller (`/Documentation/...`) for content that should render;
   use the `ency` documentation tables only when content is data-driven.
5. **Re-run the sync** after touching controllers/models:
   `bash Documentation/script/sync-docs.sh --dry-run` (then without flag if it
   would update the index).

---

## 4. Hard rules for AI assistants

- **Never replace or modify existing files without explicit permission.** Propose the
  change and explain its impact first (this is the standing `AI_ASSISTANT_GUIDELINES.tt`
  policy — it overrides "just fix it").
- **Perl module hygiene:** filename must match `package` declaration. Path
  `lib/Comserv/Controller/Foo/Bar.pm` ⇒ `package Comserv::Controller::Foo::Bar;`.
  Verify with the scan in AGENTS.md after creating/editing controllers.
- **Logging:** use `$self->logging->log_with_details($c, 'info', __FILE__, __LINE__,
  'method', "message")` — not bare `warn`/`print`.
- **Schema authority:** Result class is the source of truth. DB structure changes go
  through the app schema-compare; this DBIC version has **no `add_index`** — use
  `add_unique_constraint(name => [cols])`. Never hand-DDL.
- **Theming/compliance:** replace hardcoded colors with `var(--*)`; verify `.tt`
  renders across sites/themes.
- **Never start, stop, restart, or kill app servers** (`comserv_server.pl`,
  starman, plackup, docker web containers, systemd units) on **any** port
  (`:3001`, `:4009`, other worktree ports, prod) unless the user **explicitly**
  asks in that turn. Do not `pkill`/`kill` server PIDs “to pick up code”, do not
  launch `script/comserv_server.pl` in background, do not touch the user’s
  terminal-owned server process. Code with `-r` reloads on save when *they*
  run the server; AI verifies with HTTP/`perl -c` only. If the server is down
  or stale, **tell the user** — they restart it.
- Static asset changes (JS/JSON) apply on browser hard-refresh alone when the
  server is already running.

---

## 5. Known debt the documentation role owns (see plan)

Tracked in `.hermes/docs-maintenance-plan.md`. Measured audit (2026-08-25), full version
in the in-app `DocumentationMasterPlan.tt`:
- The **only** directory the app serves is `Comserv/root/Documentation/`. The repo-root
  `Documentation/` folder is a misplacement (duplicates + invisible `DailyPlans/`).
- 606 rendered `.tt`: only 208 (34%) use `var(--*)` theming → 398 unthemed.
- META 75%, PageVersion 80%; 24/34 modules lack `index.tt`.
- 49 stray `.md` + 62 stale `PycharmProjects` paths under the served tree.
- Two AI controllers declare `package Comserv::AI::*` (intentional Catalyst namespace).
- `AI_ASSISTANT_GUIDELINES.tt` referenced a non-existent `Documantation.pm` (fixed).
- `README.tt` / `AGENTS.md` point at legacy `Comserv/docs/` paths that no longer exist.

---

## 6. Hermes Director + Coder + models (all branches)

**Process (shared):** every todo touch uses Director (session primary) + optional
cheap Coder. Work orders live on the todo so free models do not re-plan.
Details: `.hermes/director-coder.md` (tracked; merge reaches every branch).

**Models (shared defaults + per-branch prefs):** `.hermes/models.md`.
Example: **Documentation** prefers free Director/Coder; **schema** keeps a strong
Director; coding default is SuperGrok composer + nemotron lightning free coder.
Never put API keys in git — live enforcement is `~/.hermes/config.yaml` or
`hermes -p <profile>` (e.g. `documentation`).

**Worktree rule:** each worktree is its own git root. Branch `.hermes.md` must
honor the repo-root global file, then add domain + optional “use models.md row
for this branch”. See skill `hermes-branch-context`. Launch:
`cd <worktree> && hermes chat` (never `hermes chat -w` inside a Comserv worktree).

Tracking todo: **#2350** (PLANNING).

---

## 7. Where to look next

- **Global plan coordinator (all branches/projects):** `/Documentation/MASTER_PLAN`
- **Documentation master plan (audit + work items + linked plans):**
  `/Documentation/DocumentationMasterPlan`
- In-app docs hub: `/Documentation/DevelopmentStandards`, `/Documentation/CssThemes`,
  `/Documentation/ThemeConfig`, `/Documentation/ApplicationTtTemplate`,
  `/Documentation/DocumentationTtTemplate`
- Standards source: `Comserv/root/Documentation/AI_DEVELOPMENT_GUIDELINES.tt`
- Logging checklist: `Comserv/lib/Comserv/Util/Logging.pm` header
- **Maintenance plan (audit + schedule + checklist):** `.hermes/docs-maintenance-plan.md`
- **Director+Coder process:** `.hermes/director-coder.md`
- **Model matrix (incl. Documentation free-first):** `.hermes/models.md`

---

## 8. Branch map — helpdesk WORKER DOMAIN (this worktree only)

**Identity**
- Branch: `helpdesk`
- Worktree git root: `/home/shanta/.comserv/worktrees/helpdesk/Comserv`
- App dir: `/home/shanta/.comserv/worktrees/helpdesk/Comserv/Comserv`
- Port: **4009** (registry: `Comserv/root/config/worktrees.json`)
- Host / sitename: `workstation.local` / `CSC`
- Live URL: `http://workstation.local:4009/HelpDesk`
- Parent / merge base: local **`main`** (linked worktree; same object DB as primary)
- Historical reference only (do NOT edit as live workflow): `helpdesk-7217`

**Global rules**
This worktree is its own git root — the primary file is NOT auto-loaded by parent walk.
Still honor by pointer (do not fork global rules here):
  `/home/shanta/PycharmProjects/comserv2/.hermes.md`
Process/models (local copies after seed; else primary):
  `.hermes/director-coder.md` · `.hermes/models.md`
Launch: `cd ~/.comserv/worktrees/helpdesk/Comserv && hermes chat` (never `-w`).

**Domain — HelpDesk support tickets**
Owns the support-ticket product for Comserv-hosted sites:
- Controller: `Comserv/lib/Comserv/Controller/HelpDesk.pm` (namespace `HelpDesk`)
- Templates: `Comserv/root/CSC/HelpDesk/*.tt`, `Comserv/root/CSC/HelpDesk*.tt`
- Nav: `Comserv/root/Navigation/TopDropListHelpDesk.tt`
- CSS/icon: `Comserv/root/static/css/helpdesk.css`, `…/icons/helpdesk.svg`
- Docs: `Comserv/root/Documentation/HelpDeskSystem.tt`,
  `…/guides/helpdesk-how-to-use.tt`, `…/controllers/HelpDesk.tt`
- Result classes already on main (schema owns DDL/structure):
  `Model/Schema/Ency/Result/SupportTicket.pm`, `TicketMessage.pm`
  Tables: `support_tickets`, ticket messages. New columns/tables → **schema** branch
  via schema-compare only (no hand-DDL; no `add_index`).

**Lifecycle features already in tree**
Submit / status / view / reply · admin open/closed/resolved · status update ·
reminders · auto-close stale · customer reply reopens · tickets_alert API ·
search API. Prefer extending this controller + CSC templates over new modules.

**Application relationship**
- `:3001` = primary **main** (merge target / deploy coordinator). Edits here are
  invisible on 3001 until merged via gated `merge_to_main`.
- Verify live work on **:4009** (this process cwd must be the app dir above)
  with curl/HTTP only — **do not start/stop/restart the :4009 server**; the
  human owns that process (usually `perl script/comserv_server.pl -p 4009 -r`
  in their own terminal). If reload is needed after `.pm` changes, ask them.
- Never `git switch main` / `checkout main` inside this worktree.
- Freshen from main when behind: `git merge main` **here** (local `main` ref),
  not `origin/main`. Do not ping-pong merge both directions without a reason.
- After HelpDesk work is green: human-gated merge **helpdesk → main** on 3001.

**Boundary (stay in lane)**
If a fix needs code outside HelpDesk ownership for correctness, STOP, find the
owner on the coordinator map (primary `.hermes.md` / worktrees.json), open a todo
on that branch. No AI chat core, no schema DDL, no InventoryAccounting/3d here.

**Models**
Default coding stack (`.hermes/models.md` → general): Director composer;
Coder `nemotron-3.5-lightning-free`. No separate profile required.

**Compliance checklist (every edit)**
- Propose before replacing existing files (AI_ASSISTANT_GUIDELINES).
- `package` == path; `log_with_details` only; `var(--*)` themes; modular JS via
  `js_load.tt`; changelog fragment `.inc` under `changelog/entries/`.
- `perl -c` touched `.pm`; verify on `:4009` not only `:3001`.
